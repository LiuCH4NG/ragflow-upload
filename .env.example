# RAGFlow文件上传工具配置文件
# 将除了知识库名称和上传文件路径外的配置保存到此文件中

# RAGFlow API密钥
RAGFLOW_API_KEY=your-api-key-here

# RAGFlow服务器地址
RAGFLOW_BASE_URL=http://localhost:9380

# 每批上传的文件数量（可选，默认：5）
BATCH_SIZE=5

# 是否在上传后自动启动文档解析（可选，默认：true）
AUTO_PARSE=true

# 是否跳过已存在的文件（可选，默认：false）
SKIP_EXISTING=false

# 日志文件路径（可选，如果不指定将自动生成时间戳命名的日志文件）
LOG_FILE=